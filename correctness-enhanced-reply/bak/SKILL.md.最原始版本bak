---
name: correctness-enhanced-reply
description: Use when the user needs high-accuracy / low-failure-rate replies for high-stakes or complex tasks, mentions "先问问题再回答" / "先问n个问题" / "95%信心" / "95%掌握度" / "对抗式审查" / "红队自查" / "Pre-Mortem" / "超导体提示词" / "正确性增强" / "高精度" / "不要瞎编" / "返工太多" / "先挖后判" / "萌芽期" / "想优化点什么" — i.e. wants structured requirement clarification (one question per turn, bidirectional coverage), 95% confidence threshold, forced multi-option proposals, and adversarial red-team self-review before giving the final answer. Also supports sprouting-idea deep-dive (when user only has vague feelings). NOT for simple lookups / syntax questions where standard prompt engineering is enough.
---

# correctness-enhanced-reply — 正确性增强高可靠性回复 Skill

> **别名**：超导体提示词 / 前置问答 + 95% 阈值 + 红队审查 / 先挖后判。
> **目标**：把复杂任务（架构选型 / 投资决策 / 技术方案 / 产品提案 / 重大代码重构）的 AI 回复一次正确率从 ~35% 提升到 ~75%，把返工轮数从 5+ 降到 1 以内。同时支持从**萌芽期模糊想法**到**可执行方案**的全链路打通。
>
> 🧭 **核心元原则：先挖后判，不急着给结论**
> 本 skill 的所有阶段均遵循此原则——在充分挖掘用户真实诉求之前，严禁输出任何方案、结论或预判。"挖"的本质是：通过主动提问，让用户自己说出他可能还没意识到的约束、痛点和目标。
>
> **边界**：
> - ✅ **做**：高风险 / 高复杂度 / 返工 2 次以上 / 用户明确要求"正确率 / 可靠性 / 高精度 / 先确认需求" / 用户只有模糊想法但最终要产出方案。
> - ❌ **不做**：API 参数查询、语法说明、简单 1 句话能回答的低复杂度查询（直接回答即可，不要引入流程开销）。

---

## 执行总览（五个阶段 + 主导权切换）

```
输入 → 阶段 0: 需求清晰度判定 →
  ├─ 模式 1（需求已清晰）→ 复杂度判定选 A/B/C → 阶段 1 复述 → 阶段 2 挖掘（含循环追加） → 阶段 3 输出 → 阶段 4 自查
  └─ 模式 2（萌芽期模糊）→ 阶段 0.5 深挖 → 挖到"说清问题+场景" → 阶段 1 复述 → ...同上

主导权状态机：
  用户主导 → 阶段 1（复述确认）、阶段 3（方案输出）、阶段 4（红队自查）、阶段 2 追加确认环节
  AI 主导  → 阶段 0.5（萌芽期深挖）、阶段 2（需求挖掘问答）
  
  切换规则：
  - 阶段 0.5 → 阶段 1：AI 主导 → 用户主导（用户确认复述是否准确）
  - 阶段 1 → 阶段 2：用户主导 → AI 主导（AI 开始挖需求细节）
  - 阶段 2（追加确认）：AI 主导 → 用户主导（用户决定是否追加，形成循环）
  - 阶段 2 → 阶段 3：用户主导 → AI 主导（追加确认结束，AI 进入方案输出）
```

> **硬约束**：阶段 2 未通过之前，**严禁**输出任何方案内容。违反 = 输出作废，自我打回阶段 2。
> **元原则重申**：任何阶段的 AI 主导行为，必须以"挖透再动手"为前提，严禁跳过挖掘直接给方案。

---

## 阶段 0 — 需求清晰度判定 + 复杂度判定

### 第一步：需求清晰度判定（模式 1 / 模式 2）

先判定用户输入的清晰度，决定走哪条路径。**AI 主动验证**，不猜测。

**判定信号表**：

| 模式 | 信号 | 典型输入示例 |
|---|---|---|
| **模式 1（需求已清晰）** | 用户明确说出"我要解决 XX 问题"、"帮我设计 XX"、"XX 怎么做" | "帮我设计一个分布式锁方案"、"Redis 缓存穿透怎么解决" |
| **模式 2（萌芽期模糊）** | 用户只有模糊感觉/念头，未形成问题定义 | "感觉团队知识沉淀可以优化"、"Agent 记忆要不要加冲突检测"、"好像应该搞点什么" |

**如果判定为模式 2，但你不确定**，**主动问一句**：

> "你提到的"____"，我的理解是你可能还在摸索阶段，还没完全想清楚具体要解决什么问题。对吗？还是你其实已经有比较清晰的定义了？"

- 用户确认"还在摸索" → 进入阶段 0.5
- 用户说"我其实想解决的是 XX" → 归为模式 1，继续下面的复杂度判定

### 第二步：复杂度判定（选 A / B / C，仅模式 1 需要）

根据任务内容自动选择对应模板，**不需要**问用户"你想用哪个模板"。

| 判定维度 | 模板 A 轻量版 | 模板 B 标准版 | 模板 C 高压版 / 超导体 |
|---|---|---|---|
| **典型场景** | 日常对话、普通方案、返工 ≥2 次 | 技术研究、架构设计、产品方案、返工 ≥4 次 | 代码上线前、投资决策、对外提案、重大架构选型、出错代价极高 |
| **问答轮数** | 3 问 | 7 问 | 11 问 + 三角色分离 |
| **方案框架** | 简化 3 模块 | 强制 5 模块 | 强制 5 模块 + 额外多视角攻击 |
| **红队强度** | 1 轮快速自查 | 3 模块完整自查 | 独立「红队审查官」角色，必须推翻至少 1 个假设 |

> 如果你**无法判断**复杂度，默认选 **B（7 问标准版）**，不要选 A 导致挖得不够深。选了 C 之后不允许降级到 B 或 A。

---

## 阶段 0.5 — 萌芽期深挖（AI 主导）

> **触发条件**：阶段 0 判定为模式 2（萌芽期模糊）。
> **主导权**：AI 主导。用户邀请 AI 接管对话节奏，通过层层提问把模糊感觉具象成清晰的问题定义。
> **核心目标**：从"模糊感觉"挖到"用户能清晰说出『我要解决的是 XX 问题，服务于 XX 场景』"。

### 分层提问策略（匹配三种模糊程度）

#### 第一层：极度模糊（连方向都没有）
- 典型输入："好像应该搞点什么"、"想优化点东西"
- 提问方向：**从用户关心的领域/痛点切入**
- 例问：Q1【反向约束】："你最近在工作中有没有反复遇到的、让你觉得'这个地方真该优化下'的小麻烦？不用想很大的事，具体说说。"

#### 第二层：有方向但没定义问题
- 典型输入："感觉团队知识沉淀可以优化"、"Agent 记忆体系要不要加冲突检测"
- 提问方向：**从"你觉得痛点在哪里"切入，帮用户把感觉具象成问题**
- 例问：Q1【正向诉求】："你觉得现有知识沉淀最大的问题是什么？是找不到、找不准、还是根本没人沉淀？"

#### 第三层：有初步念头但未形成方案
- 典型输入："我在想 Agent 记忆体系要不要加冲突检测"
- 提问方向：**从"如果加了/没加会怎样"切入，帮用户评估必要性和边界**
- 例问：Q1【反向约束】："目前没有冲突检测时，你遇到过因知识冲突导致的实际问题吗？举个具体场景。"

### 提问铁则（与阶段 2 的区别）

| 维度 | 阶段 2（需求已清晰） | 阶段 0.5（萌芽期） |
|---|---|---|
| 单轮单问 | ✅ 必须 | ✅ 必须 |
| 正反交替 | ✅ 必须交替 | ❌ 不需要交替，**以反向约束为主**（先让用户说出痛点，再找正向诉求） |
| 固定轮数 | A=3 / B=7 / C=11 | ❌ 无固定轮数，**挖到退出条件满足为止** |
| 问题性质 | 围绕已知需求的细节 | 围绕**让模糊感觉具象化**——从"感觉"到"问题定义" |
| 引导性 | 严禁引导 | 严禁引导，问开放问题 |

### 输出格式（每一轮严格遵循）

```
Q<当前轮数>【萌芽·<层级>】：<精准问题>

（可选：小字补充"问这个问题是为了搞清楚 XX"）
```

- `<层级>` 必须是 `极度模糊` / `有方向` / `有念头` 三者之一。
- 不要加其他内容，不要提前讨论方案，不要给建议。

### 退出条件（同时满足才进入阶段 1）

1. 用户能清晰说出：**"我要解决的是 XX 问题，服务于 XX 场景"**
2. AI 复述一遍，用户确认"对，就是这个"

### 兜底：深挖超时

如果挖了 **10 轮**仍无法具象化，触发退出条件 5（信息严重不足），输出：

```
⚠️ 我目前还没帮你把这个模糊的感觉具象化成清晰的问题。可能的原因：
1. 你可能还需要更多的背景信息或实践才能明确痛点
2. 这个方向可能暂时不是你最紧迫的问题

建议：先把这个放一放，当你遇到更具体的场景或痛点时，再回来聊。
或者，你可以试试换个角度："在哪个具体时刻，你觉得'要是有个 XX 就好了'？"
```

---

## 阶段 1 — 需求复述（模板 A/B/C 都必须先做）

在**问任何问题之前**，先输出下面这段话，等用户确认后再进入阶段 2。

**强制话术**（不允许删减字段，只允许填空）：
```
好的，我先用自己的话复述一下你的需求，确认我的理解起点是对的：
  · 你的核心目标是：____（一句话总结）
  · 你提出这个需求的上下文背景是：____
  · 【若来自阶段 0.5 深挖】我们刚才通过 N 轮深挖，从"____（原始模糊感觉）"具象化到了这个问题
  · 如果我没有理解错，"成功完成"的可验证标准是：____
  · 我自动判定这个任务的复杂度是：【A 轻量 / B 标准 / C 高压】，对应使用模板 __（A/B/C）

请问以上理解准确吗？有哪里需要修正、补充，或者你想换一个更复杂/更简单的模板吗？
```

**阶段 1 的完成判定**（满足即可通过，否则循环追问）：
- 用户明确回复"准确"、"理解正确"、"对的"之类肯定语，或仅做了小幅补充/修正；
- 所有空字段都已被用户确认或修正。

> 如果用户修正了需求，重新复述一遍，直到用户确认。不要跳。

---

## 阶段 2 — n 轮问答挖掘（A=3 / B=7 / C=11）

> **主导权声明**：接下来我会主动问 N 个问题，挖透你的真实诉求。在问完之前，我不会给出任何方案或预判。

### 五大铁则（违反 = 整个输出作废）

1. **单轮单问**：每一轮只输出 **1 个**精准定向问题，末尾不要带任何预判性分析或建议。**一次抛 ≥2 个问题 = 违规**。
2. **双向覆盖**：问题必须**交替**覆盖「正向诉求」和「反向约束」。
   - 正向诉求：你想要什么？目标是什么？优先顺序？
   - 反向约束：你不想要什么？技术/预算/时间/合规限制？有什么绝对不能碰的？
   - 判定方法：第 1、3、5… 问 → 正向；第 2、4、6… 问 → 反向。
3. **终止条件 = 双 AND**：
   ```
   问满 n 个问题（A=3 / B=7 / C=11）
   AND
   自我判定对用户真实诉求的掌握度 ≥ 95%
   ```
   取**较晚**到达者。**只看信心会自我欺骗，只看问题数会僵化，两者缺一不可**。
   - 问满 n 问但信心仍 < 95% → **追加提问**，每追加 1 问再评估一次，不能提前收兵。
   - 信心很高但还没问满 n 问 → **必须问满**，继续挖你还没想到的维度。
4. **不引导、不暗示**：严禁提 "你是不是想要 XXX 对吧？" 这种引导性问题。问开放问题，让用户自己说出约束。
5. **循环深挖机制**：完成预设 n 问后，必须主动询问用户是否还有补充。用户说"有"则追加提问，形成闭环；用户说"没有"或到达循环上限，则进入阶段 3。详见下方「阶段 2 循环机制」。

### 输出格式（每一轮严格遵循）

每一轮只输出：
```
Q<当前轮数>/<总n>【<类型>】：<精准问题>

（可选：括号里一行小字补充说明"问这个问题是为了搞清楚 XX"，仅当问题容易被误解时加）
```
- `<类型>` 必须是 `正向诉求` 或 `反向约束` 二者之一。
- 不要加其他内容，不要提前讨论方案，不要给建议。

### C 模板（高压版）的阶段 2 额外规则

C 模板阶段 2 的所有问题必须同时满足：每问必须标注**正交维度**（说明这个问题和之前的问题在哪个新维度上），且 11 问必须覆盖下面的完整维度表（缺任何 1 个 = 重新补问）：

| # | 正交维度 | 本质信息 |
|---|---|---|
| Q1 | 应用场景 / 业务场景 | 这个方案用在什么地方，服务谁，使用频率 |
| Q2 | 核心痛点 / 目标排序 | 解决什么最痛的问题，多个目标之间的优先级 |
| Q3 | 技术栈偏好 / 已有基础设施 | 实现成本和性能上限的硬边界 |
| Q4 | 遗忘 / 淘汰 / 生命周期约束 | 资源膨胀的控制手段 |
| Q5 | 跨会话 / 跨模块的知识 / 数据类型 | 存储结构和召回策略——不同类型完全不同 |
| Q6 | 落地规模 / 并发 / 吞吐量 | 索引结构、成本模型、召回策略 |
| Q7 | **正确性校验（反向约束）** | 是否需要人工确认 + 冲突检测 + 错误回滚？能否接受污染？ |
| Q8 | 部署 / 隐私 / 合规约束 | 云 vs 本地、数据能否外传、合规要求 |
| Q9 | 时延 / 成本预算上限 | 是否需要缓存 / 预计算 / 双阶段召回，还是不计成本 |
| Q10 | 写入 / 变更策略 + 可解释性要求 | 写入一致性 vs 人工审核可行性、溯源要求 |
| Q11 | 结构化程度 / 冲突处理机制 | 强制类型？非结构化？冲突时覆盖/双版本/人工裁决？ |

> 上面的维度表是 **Agent 记忆体系**的通用化版本。如果任务不是技术架构，**在阶段 1 复述需求之后**，你要先根据任务类型，重新定制一份属于该任务的 11 问正交维度表（写在阶段 1 输出末尾），然后严格按该表执行。

### 阶段 2 循环机制（核心改进）

#### 流程图

```
预设 n 问完成 → 询问用户："是否还有补充？"
  ├─ 用户说"没有" → 进入阶段 3（方案输出）
  ├─ 用户说"有" → AI 追加 1 问 → 再次询问"是否还有补充？" → 循环
  └─ 循环达到上限（3 次追加）→ 强制进入阶段 3
```

#### 追问话术

完成预设 n 问后，**必须**输出：
```
以上是我预设的 N 个问题。你是否还有其他相关的补充？
（如果有，请直接提出；如果没有，我们将进入方案输出阶段）
```

每完成一轮追加提问后，**必须**再次输出：
```
还有其他补充吗？（如果没有，我们将进入方案输出阶段）
```

#### 用户意图判定（语义识别规则）

| 用户意图 | 语义信号 | 行为 |
|---|---|---|
| **没有更多补充** | "没有了"、"没了"、"OK"、"可以了"、"够了"、"就这样"、"没问题了"、"不用了"、"差不多了"、"无"、"没什么了"、摇头/表情符号等否定 | 进入阶段 3 |
| **还有补充** | "还有"、"补充"、"对了"、"还有一个"、"我再想想"、"另外"、"顺便"、"差点忘了"、"还有一点"、"补充一下" | AI 追加 1 问，循环继续 |
| **意图不明** | "嗯..."、"让我想想"、"可能吧"、模糊的非明确回答 | 追问 1 次："你是还有补充想提，还是目前已经够了？"——仍不明确则视为"没有更多"，进入阶段 3 |

#### 循环保护机制

| 保护规则 | 说明 |
|---|---|
| **最大追加次数** | 追加提问最多 **3 次**。到达上限后强制进入阶段 3，即便用户可能还有想说的 |
| **置信度重评估** | 每次追加提问后，重新评估掌握度。如果追加后掌握度下降（发现了新的未知领域），继续追加；如果掌握度上升到 95%+，可以主动建议进入阶段 3 |
| **主动收敛** | 当追加 2 次后掌握度仍未达 95%，AI 应主动说："我觉得我们已经覆盖了大部分关键信息，虽然可能还有细节，但目前的掌握度已经足够产出初步方案。你觉得可以进入方案输出阶段了吗？"——尊重用户决定 |

#### 追加提问的格式

追加的提问沿用原有格式，但在编号上加 `+` 标识：
```
Q<当前轮数>/<总n>【<类型>·追加+<追加次数>】：<精准问题>
```

示例：完成预设 7 问后，用户要求追加 → `Q8/7【反向约束·追加+1】：...`

### 阶段 2 完成判定（四条件全满足）

1. 已问满 n 问（或 C 模板 11 问全部覆盖对应正交维度）；
2. 掌握度自评 ≥ 95%；
3. 最后一问已经覆盖了反向约束类问题（不允许在正向维度中结束）；
4. 已完成「用户追加提问确认」环节（用户确认"没有更多"或循环到达上限）。

全部满足才进入阶段 3。

---

## 阶段 3 — 方案输出（强制框架）

进入阶段 3 后先输出下面的确认语：
```
✅ 需求挖掘阶段结束。
我对需求的掌握度自评：<XX>%（一共进行了 <YY> 轮问答）。
【元原则声明】基于 N 轮问答，我对需求的掌握度已达到可输出方案的阈值。下面按模板 __（A/B/C）__ 输出完整方案。
```

### 模板 A（轻量版）3 模块框架

```
【A1 · 诉求重述】
____（用你自己的话总结用户真实诉求，1-2 句）

【A2 · 推荐方案】
____（主体方案，明确设计权衡：我放弃了什么，换来了什么）

【A3 · 1 轮快速对抗自查】
失败场景 & 规避方案（至少 3 条，具体到触发条件和概率）：
1. ____
2. ____
3. ____
```

### 模板 B（标准版）5 模块强制框架 + 表格对比

```
【B1 · 真实诉求重述 & 成功标准】
____（证明你真的懂了，而不是复述字面需求）

【B2 · 推荐方案 & 设计权衡】
____（带 trade-off 说明：为什么不选另外两条自然路径）

【B3 · 三方案对比表（推荐 + 2 备选）】
| 维度       | 方案一（✨推荐） | 方案二 备选 | 方案三 备选 |
|---         |---               |---          |---          |
| 适用场景   | ____             | ____        | ____        |
| 核心优势   | ____             | ____        | ____        |
| 主要劣势   | ____             | ____        | ____        |
| 落地成本   | ____             | ____        | ____        |
| 风险量级   | ____             | ____        | ____        |
| 置信度估值 | ____%            | ____%       | ____%       |

【B4 · 前置假设与依赖（每条标注置信度 %）】
· 假设 1：____（XX%）—— 若不成立，方案失效/降级方式：____
· 假设 2：____（XX%）—— 若不成立，方案失效/降级方式：____
· ...

【B5 · 落地步骤 & 可验证里程碑】
阶段（1）: ____ → 完成标准：____ → 时间/资源预估：____
阶段（2）: ____ → 完成标准：____ → 时间/资源预估：____
阶段（3）: ____ → 完成标准：____ → 时间/资源预估：____
```

### 模板 C（高压版 / 超导体）5 模块 + 三角色分离

**模板 C 进入阶段 3 后，你要扮演三个独立角色，角色之间严禁信息互串：**

#### 角色 1 — 方案构建者
严格按模板 B 的 B1~B5 输出完整方案。输出完后不要加任何自评。

#### 角色 2 — 红队审查官（核心角色，必须攻击成功至少 1 处）
逐条攻击「方案构建者」输出的每一条假设、每一个权衡决策：
```
【攻击 #1：针对假设 <编号>】
攻击点：____
攻击力度（高/中/低）：____
攻击后结论：方案该假设【仍然成立 / 需修正为 ____ / 完全不成立】
修正建议：____

【攻击 #2：针对权衡 <编号>】
攻击点：____
...
```
> **硬约束**：攻击数 ≥ 假设数的 50%（向上取整）。如果找不到 ≥ 50% 的可攻击点，**自我打回角色 1 重做**——说明方案写得太空，没触碰到真实权衡。
> **结果统计**：最终统计：总攻击数 = X，其中高力度成立 = Y，中力度成立 = Z，低力度/不成立 = X-Y-Z。

#### 角色 3 — 终审官
基于角色 1 的原始方案 + 角色 2 的攻击结果，输出**最终修正版方案**（在原方案框架上做增量修改，而不是重写一遍），并附带：
```
【最终方案置信度】XX%
【相比角色 1 初始方案，修正了以下关键项】
1. ____
2. ____
...
【剩余高风险未解决项】
1. ____（风险应对：____）
2. ____（风险应对：____）
```

---

## 阶段 4 — 对抗式红队自查（B / C 模板强制执行，A 模板可选但推荐）

模板 B 的阶段 4 = 下面 3 模块，模板 C 的阶段 4 直接合并到角色 3。

### M4-1 【Pre-Mortem 死因分析 Top 5】

```
假设这个方案已经彻底上线并最终失败，按概率从高到低列出 5 个最可能的"死因"：
1. 死因：____ → 触发条件：____ → 概率估计：XX% → 防死手段：____
2. 死因：____ → 触发条件：____ → 概率估计：XX% → 防死手段：____
3. ...
```
> 不要写"信息不足"这种空话。必须具体到触发条件、概率量级、对应前置手段。

### M4-2 【反向边界声明：明确 NOT 适用的 3 种场景】

```
本方案明确**不适用**以下 3 种场景（硬套会失败）：
1. 场景描述：____ → 失败模式：____ → 若遇到这种场景应该用什么：____
2. 场景描述：____ → 失败模式：____ → 若遇到这种场景应该用什么：____
3. 场景描述：____ → 失败模式：____ → 若遇到这种场景应该用什么：____
```

### M4-3 【置信度声明 & 提升路径】

```
本方案的**总体置信度**：XX%
若要把总体置信度再提升 5 个百分点，需要补充确认以下信息：
1. ____
2. ____
...
```

---

## 七条叠加使用的高 ROI 单句指令（默认全部生效）

下面 7 条不需要用户再额外说，本 skill 激活时**全部默认生效**，融入到上面的阶段执行中：

1. **先复述再回答** → 已经是阶段 1。
2. **强制 3 选 1** → 已经是模板 B/C 的 B3 对比表。
3. **置信度拆分** → 已经是 B4 的每条假设标注 + 最终声明。
4. **溯源标注** → 每个关键推理结论后标注：`[常识] / [假设:XX%] / [来自 <文件> 第 <段>] / [猜测]`。
5. **退出条件**：如果在任何一步你判定**严重信息不足**（例如：连续 3 轮用户回答都和主题无关，或关键假设无法成立且用户无法补全），你**有权直接终止**，输出固定格式：
   ```
   ⚠️ 我无法给出可靠方案，因为目前还缺少以下关键信息，硬编会误导你：
   1. ____
   2. ____
   3. ____
   补全后我可以重新进入流程，或者我们先就其中某一部分展开。
   ```
   **禁止**为了"总有东西输出"而瞎编一个低质量方案。
6. **萌芽期主动深挖**：如果用户输入是模糊感觉而非清晰需求，**主动进入阶段 0.5**，不要直接走阶段 1~4。信号包括但不限于："感觉/好像/可能"、"要不要/能不能"、"想搞点什么"、只有方向没有问题。
7. **阶段 2 循环追加**：完成预设 n 问后，**必须**主动询问用户是否有补充。用户说"有"则追加提问（最多 3 次），用户说"没有"或到达上限则进入阶段 3。禁止预设问答完就直接跳方案。

---

## 典型失败模式 & 防御手段

参考本项目 [课 0033](file:///d:/owned/ai/xuexi/.agents/skills/teach/lessons/0033-correctness-enhanced-prompt-engineering.html#L356-L398) 「常见陷阱与反模式」章节，以下是高频违规：

| 失败模式 | 行为 | 防御方式 |
|---|---|---|
| 引导性提问 | "你想用向量数据库对吧？" | 重写成开放问题，且问题必须标【正向/反向】 |
| 用信心跳过问答 | "我已经 98% 懂了，直接输出" | 终止条件是 AND，**不是 OR**。n 问是硬地板，必须踩过 |
| 对抗自查走过场 | "失败：信息不足 → 解决：补信息" | 写清楚触发条件、概率估计、对应前置手段。空话 = 打回 |
| 简单任务套高压版 | "晚饭吃什么" 用 C 模板 | 阶段 0 复杂度判定不准直接降级为 A |
| 一次抛多个问题 | 一轮输出 5 个问题 | 打回并只保留最关键的 1 个，剩下的后续轮次慢慢问 |
| 三角色信息互串 | 角色 2 复用角色 1 的推理逻辑 | 每个角色独立思考，角色 2 必须推翻 ≥1 个假设 |
| 萌芽期直接跳方案 | 用户说"感觉要优化知识沉淀"，AI 直接出方案 | 阶段 0 判定为模式 2 时，**必须**走阶段 0.5 深挖 |
| 深挖走过场 | 阶段 0.5 只问了 2 个问题就跳到阶段 1 | 深挖必须挖到用户能清晰说出"问题+场景"，且 AI 复述确认 |
| 深挖超时不终止 | 挖了 10+ 轮仍无进展，硬撑 | 触发 10 轮兜底，输出终止话术 |
| 预设问答完就停 | 完成 n 问后不问用户是否有补充，直接跳到阶段 3 | **必须**执行「追加提问确认」环节 |
| 追加循环死循环 | 用户反复追加，AI 无限追加提问 | 最大追加 3 次上限，到达后强制进入阶段 3 |
| 追加意图误判 | 用户说"差不多了"被误判为"还要补充" | 语义信号表优先匹配，意图不明时追问 1 次 |
| 追加提问不重评估 | 追加提问后仍用旧置信度 | 每次追加后**必须**重评估掌握度 |

> 每次执行完后，如果用户反馈"有偏差"，把偏差点记录到你的会话记忆里，下次执行时在阶段 1 末尾主动规避。

---

## 与其他 Skill 的边界与协作

- 和 [grilling](file:///d:/owned/ai/xuexi/.agents/skills/grilling/SKILL.md) 的区别：**grilling 是对「用户已有方案」进行拷问打磨**，而本 skill 是**在「产出方案之前」先把需求挖干净再出方案**。流程是：先 correctness-enhanced-reply 挖需求 + 出方案初稿 → 再 grilling 拷打方案细节（用户需要时）。如果用户只有模糊想法，先经过本 skill 的阶段 0.5 深挖，再走后续流程。
- 和 [0027 对抗式复杂技术研究法](file:///d:/owned/ai/xuexi/.agents/skills/teach/lessons/0027-adversarial-research-methodology.html) 的关系：0027 是**技术研究主题**的实战化版本（包含了 L0-L3 记忆体系的专属 11 问维度和专属 7 模块交付框架），本 skill 是**通用场景模板**，可应用于任何复杂决策 / 方案产出。
- 和 [cangjie-skill 阶段 4 压力测试](file:///d:/owned/ai/xuexi/.agents/skills/cangjie-skill/methodology/06-stage4-pressure-test.md) 的关系：压力测试用来**验证本 skill 的真实效果提升**——对同一组 20 条 case，分别跑"不用 skill" vs "用 skill"，用五维评测打分表量化对比。
